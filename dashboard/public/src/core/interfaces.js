export const ChartTypes = Object.freeze({
  TEMPORAL: 'temporal'
});

export const DataSeries = Object.freeze({
  TOTAL: 'total',
  MALE: 'male',
  FEMALE: 'female'
});

export const Events = Object.freeze({
  COVID_YEAR: 2020
});
